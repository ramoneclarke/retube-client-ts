import Sidebar from "../Sidebar";

const meta = {
  title: "Layout/Sidebar",
  component: Sidebar,
};

export default meta;

export const DefaultSidebar = {
  args: {
    darkMode: false,
    path: "/",
  },
};
