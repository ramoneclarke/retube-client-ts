import UserAccountButton from "../UserAccountButton";

const meta = {
  title: "Layout/UserAccountButton",
  component: UserAccountButton,
};

export default meta;

export const DefaultAccountButton = {
  render: () => <UserAccountButton />,
};
